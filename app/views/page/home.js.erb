$('#my-posts').append('<%= j render @posts %>');
<% if @posts.next_page %>
  $('.pagination').html('<%= j will_paginate @posts %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
  $('#load-ico').remove();
<% end %>